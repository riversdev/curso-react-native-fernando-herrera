import { StyleSheet } from 'react-native'

export const appTheme = StyleSheet.create({
    container: {
        marginHorizontal: 20,
    },
    title: {
        fontSize: 30,
        marginBottom: 10,
    },
})